<form method="post" action="{{ path('app_reservacion_delete', {'idReservacion': reservacion.idReservacion}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservacion.idReservacion) }}">
    <button class="btn">Delete</button>
</form>
